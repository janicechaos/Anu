import MoodSlider from '@/components/MoodTracker';
import { StyleSheet } from 'react-native';

export default function HomeScreen() {
  return (
    <MoodSlider />
  );
}

const styles = StyleSheet.create({
  // Empty styles object for consistency
});
